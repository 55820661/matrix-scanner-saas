# تقارير التنفيذ - Matrix Scanner SaaS Corrected Plan

## Sprint C1 - Current State and Documentation Alignment

Sprint المنفذ:
`Sprint C1 - Current State and Documentation Alignment`

الهدف:
تثبيت مرجعية التوثيق قبل أي تنفيذ برمجي، والتأكد أن مراجع الخطة المصححة أصبحت tracked وجاهزة للاعتماد في التنفيذ Sprint by Sprint.

ما تم إنجازه:
- إضافة روابط المراجع المصححة في `README.md`.
- إضافة قسم `Corrected Roadmap Authority` في `PLANS.md`.
- إضافة مراجع الخطة المصححة وسجل القرارات في `docs/DECISIONS.md`.
- اعتماد `docs/planning/DECISION-REGISTER.md` كمرجع القرارات الرسمي.
- تثبيت أن `docs/planning/CORRECTED-EXECUTION-PLAN.md` هو المرجع التنفيذي الأعلى بعد `docs/planning/ROADMAP-CORRECTION.md`.
- تثبيت أن أول Sprint برمجي فعلي بعد التوثيق هو `Sprint C1.5 - Remote Bootstrap Runtime Completion`.
- إضافة مراجع التخطيط الرسمية إلى Git.

الملفات التي تم تعديلها:
- `README.md`
- `PLANS.md`
- `docs/DECISIONS.md`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/ROADMAP-CORRECTION.md`
- `docs/planning/CORRECTED-EXECUTION-PLAN.md`
- `docs/planning/DECISION-REGISTER.md`
- `docs/planning/خطة_تصحيح_المسار_Matrix_Scanner_SaaS.docx`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
لا توجد. `makemigrations --check --dry-run` أعطى `No changes detected`.

الاختبارات التي تم تشغيلها:
- `.\.venv\Scripts\python.exe manage.py check`
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
- `.\.venv\Scripts\python.exe manage.py test --noinput`
- `git diff --check`

نتائج الاختبارات:
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات.
- full test suite نجح: 292 tests, 4 skipped.
- `git diff --check` نجح مع تحذيرات line endings فقط.

المشاكل التي ظهرت وكيف تم حلها:
- أول تشغيل كامل للاختبارات وصل إلى `OK` لكن shell wrapper انتهى بـ timeout بعد طباعة النتائج. أُعيد التشغيل بمهلة أطول وخرج بنجاح.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم الالتزام بقرارات `docs/planning/DECISION-REGISTER.md`.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`docs: align corrected roadmap and decision register`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد إضافة الملفات الرسمية إلى Git وإنشاء commit مستقل.

هل تم الانتقال تلقائيًا إلى Sprint التالي أم تم التوقف لقرار؟
يمكن الانتقال تلقائيًا إلى `Sprint C1.5 - Remote Bootstrap Runtime Completion` إذا اكتمل commit بنجاح ولم تظهر ملفات رسمية untracked.

## Sprint C1.5 - Remote Bootstrap Runtime Completion

Sprint المنفذ:
`Sprint C1.5 - Remote Bootstrap Runtime Completion`

الهدف:
استكمال Remote Bootstrap بحيث يثبت Runtime/Agent الحقيقي القادر على registration وheartbeat وpolling وAgentJob execution وjob result submission، بدل bootstrap demo bundle الذي كان يعمل registration + heartbeat فقط.

ما تم إنجازه:
- تحديث `build_runtime_archive()` ليضم ملفات `scanner_runtime` الحالية داخل tarball بدل bundle مصغر.
- تحديث `agent_service.py` المولد داخل bootstrap archive ليستخدم runtime الحالي من `scanner_runtime.prototype`.
- إضافة loop يقوم بـ:
  - registration عند عدم وجود `agent_token`.
  - حفظ `agent_token` ومسح `registration_token`.
  - heartbeat.
  - polling.
  - تنفيذ job عبر allowlist الحالي.
  - submit result.
- إضافة `runtime_mode = polling_agent` إلى config المرفوع أثناء bootstrap.
- الإبقاء على install path `/opt/matrix_scanner`.
- الإبقاء على systemd service `matrix-scanner-agent.service`.
- الإبقاء على Remote Bootstrap كمسار Matrix Admin-only.
- إضافة اختبارات تثبت أن archive يحتوي runtime الحقيقي وأن service يشير إلى agent service الصحيح.

الملفات التي تم تعديلها:
- `apps/bootstrap/runtime_bundle.py`
- `apps/bootstrap/services.py`
- `tests/unit/test_sprint3_bootstrap.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
لا توجد. `makemigrations --check --dry-run` أعطى `No changes detected`.

الاختبارات التي تم تشغيلها:
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint3_bootstrap --noinput`
- `.\.venv\Scripts\python.exe manage.py check`
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
- `.\.venv\Scripts\python.exe manage.py test --noinput`
- `git diff --check`

نتائج الاختبارات:
- focused bootstrap tests نجحت: 13 tests.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات.
- full test suite نجح: 294 tests, 4 skipped.
- `git diff --check` نجح مع تحذيرات line endings فقط.

المشاكل التي ظهرت وكيف تم حلها:
- أول محاولة لاختبارات bootstrap فشلت قبل تشغيل الاختبارات بسبب PostgreSQL connection timeout أثناء إنشاء test DB. أُعيدت المحاولة.
- محاولة ثانية وصلت إلى `OK` لكن shell wrapper انتهى بـ timeout أثناء/بعد cleanup. أُعيدت بمهلة أطول وخرجت بنجاح.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم الالتزام بقرارات `docs/planning/DECISION-REGISTER.md`.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`feat: complete sprint c1.5 remote bootstrap runtime`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد نجاح الاختبارات وإنشاء commit مستقل.

هل تم الانتقال تلقائيًا إلى Sprint التالي أم تم التوقف لقرار؟
يمكن الانتقال تلقائيًا إلى `Sprint C2 - Safe Context Builder MVP` إذا اكتمل commit بنجاح ولم تظهر قرارات ناقصة.

## Sprint C2 - Safe Context Builder MVP

Sprint المنفذ:
`Sprint C2 - Safe Context Builder MVP`

الهدف:
إضافة Context Builder آمن ومحدد لاستخدامه لاحقًا بواسطة الشات أو الـ AI planner، بدون أي live AI أو تنفيذ أدوات أو إنشاء AgentJob مباشر.

ما تم إنجازه:
- إضافة app جديد باسم `apps.ai_context`.
- إضافة خدمة `build_safe_context()` لإنتاج JSON آمن ومحدد الإصدار.
- تضمين ملخصات آمنة للحساب، السيرفر، baseline، التطبيقات، الخدمات، الدومينات، مصادر اللوج، findings، reports، knowledge، recommendations.
- تضمين metadata آمنة عن ToolRuns الحديثة بدون `result_redacted` أو raw AgentJob output.
- تضمين `available_tools` بناءً على ToolDefinition وToolPolicy وPlanTool والاشتراك والدور وحالة السيرفر.
- تطبيق tenant scope بحيث لا يرى customer user حسابًا أو سيرفرًا خارج حسابه.
- تطبيق redaction/caps على النصوص والقوائم.
- إضافة اختبارات تغطي scoping وredaction ومنع raw outputs وتوفر الأدوات حسب policy/role.

الملفات التي تم تعديلها:
- `apps/ai_context/__init__.py`
- `apps/ai_context/apps.py`
- `apps/ai_context/services.py`
- `scanner_platform/settings.py`
- `tests/unit/test_safe_context_builder.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
لا توجد. `makemigrations --check --dry-run` أعطى `No changes detected`.

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_safe_context_builder --noinput`
  - اختبار مركز للملفات والخدمة الجديدة.
- `.\.venv\Scripts\python.exe manage.py check`
  - تحقق Django العام المطلوب لكل Sprint.
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
  - للتأكد من عدم وجود migrations غير متوقعة.
- `.\.venv\Scripts\python.exe manage.py test --noinput`
  - تم تشغيل full suite لأن Sprint C2 يمس redaction وpermissions وtenant-scoped context، وهي بوابة regression أمنية.
- `git diff --check`
  - تحقق whitespace.

نتائج الاختبارات:
- focused safe context tests نجحت: 6 tests.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات.
- full test suite نجح: 300 tests, 4 skipped.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغل:
لا توجد اختبارات مطلوبة تم تجاوزها في هذا Sprint. تم تشغيل full suite بسبب طبيعة التغيير الأمنية.

المشاكل التي ظهرت وكيف تم حلها:
- ظهر تعارض في اختبار helper tool بسبب استخدام key موجود مسبقًا من seed tools. تم تغيير test helper لاستخدام `context_test_tool` لتجنب الاصطدام مع البيانات الرسمية.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم الالتزام بقرارات `docs/planning/DECISION-REGISTER.md`.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`feat: add safe context builder mvp`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد نجاح الاختبارات والتحقق من عدم وجود migrations غير متوقعة.

هل يمكن الانتقال للـ Sprint التالي؟
نعم. يمكن الانتقال إلى `Sprint C3 - Admin Chat Data Model and Read-only UI` بعد إنشاء commit مستقل لـ C2.

## Sprint C3 - Admin Chat Data Model and Read-only UI

Sprint المنفذ:
`Sprint C3 - Admin Chat Data Model and Read-only UI`

الهدف:
إضافة نموذج بيانات وواجهة Portal أولية للشات المحفوظ والمقروء، بدون AI live وبدون tool execution.

ما تم إنجازه:
- إنشاء app جديد `apps.ai_chat`.
- إضافة `AdminChatSession`.
- إضافة `AdminChatMessage`.
- إضافة `AdminChatDecision`.
- إضافة Django Admin لعرض سجلات الشات redacted.
- إضافة Portal routes لقائمة الشات، إنشاء جلسة، تفاصيل الجلسة، وحفظ رسالة user.
- السماح لـ owner/operator بإنشاء جلسات وإضافة رسائل.
- السماح لـ viewer بالقراءة فقط ومنعه من start/post.
- تطبيق account scoping على الجلسات والسيرفرات والتطبيقات.
- تخزين النصوص والmetadata وcontext snapshot بعد redaction فقط.
- التأكد من أن C3 لا ينشئ ToolRun أو AgentJob.

الملفات التي تم تعديلها:
- `apps/ai_chat/__init__.py`
- `apps/ai_chat/apps.py`
- `apps/ai_chat/models.py`
- `apps/ai_chat/services.py`
- `apps/ai_chat/admin.py`
- `apps/ai_chat/migrations/0001_initial.py`
- `apps/portal/views.py`
- `apps/portal/urls.py`
- `scanner_platform/settings.py`
- `templates/portal/base.html`
- `templates/portal/chat/list.html`
- `templates/portal/chat/detail.html`
- `tests/unit/test_admin_chat.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
نعم. تمت إضافة `apps/ai_chat/migrations/0001_initial.py` لنماذج الشات الجديدة فقط.

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_admin_chat --noinput`
  - اختبارات مركزة للنماذج والخدمة وPortal chat permissions.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint6_portal --noinput`
  - regression للـ Portal لأن C3 أضاف routes وnavigation وسلوك صلاحيات.
- `.\.venv\Scripts\python.exe manage.py check`
  - تحقق Django العام المطلوب.
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
  - للتأكد من عدم وجود migrations غير متوقعة بعد migration المقصودة.
- `.\.venv\Scripts\python.exe manage.py test --noinput`
  - تم تشغيل full suite لأن C3 يغير Portal permissions وredaction-sensitive storage وtenant access.
- `git diff --check`
  - تحقق whitespace.

نتائج الاختبارات:
- focused admin chat tests نجحت: 7 tests.
- Portal regression tests نجحت: 16 tests.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات إضافية.
- full test suite نجح: 307 tests, 4 skipped.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغل:
لا توجد اختبارات مطلوبة تم تجاوزها في هذا Sprint. تم تشغيل full suite كـ security/permissions gate.

المشاكل التي ظهرت وكيف تم حلها:
لا توجد مشاكل تنفيذية بعد إضافة C3؛ الاختبارات المركزة والعامة نجحت.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم الالتزام بقرارات `docs/planning/DECISION-REGISTER.md`.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`feat: add admin chat mvp`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد نجاح الاختبارات وإنشاء migration المقصودة فقط.

هل يمكن الانتقال للـ Sprint التالي؟
نعم. يمكن الانتقال إلى `Sprint C4 - Deterministic Chat Responder` بعد إنشاء commit مستقل لـ C3.

## Sprint C4 - Deterministic Chat Responder

Sprint المنفذ:
`Sprint C4 - Deterministic Chat Responder`

الهدف:
إضافة responder deterministic داخل الشات يعتمد فقط على Safe Context، بدون live AI وبدون تشغيل أدوات.

ما تم إنجازه:
- إضافة intent routing deterministic لأسئلة status وfindings وreports وavailable tools وsummary.
- توليد رد assistant من `build_safe_context()` فقط.
- حفظ رد assistant كـ `AdminChatMessage` redacted.
- حفظ قرار الرد في `AdminChatDecision`.
- تحديث Portal chat POST ليحفظ رسالة المستخدم ثم يولد رد assistant.
- إبقاء C4 بدون live AI provider وبدون ToolRun وبدون AgentJob وبدون Telegram.

الملفات التي تم تعديلها:
- `apps/ai_chat/services.py`
- `apps/portal/views.py`
- `tests/unit/test_admin_chat.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
لا توجد. `makemigrations --check --dry-run` أعطى `No changes detected`.

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_admin_chat --noinput`
  - اختبارات مركزة للـ deterministic responder وdecision logging وredaction وعدم إنشاء ToolRun/AgentJob.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint6_portal --noinput`
  - regression للـ Portal لأن C4 يغير POST behavior في صفحة الشات.
- `.\.venv\Scripts\python.exe manage.py check`
  - تحقق Django العام.
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
  - للتأكد من عدم وجود migrations غير متوقعة.
- `.\.venv\Scripts\python.exe manage.py test --noinput`
  - تم تشغيل full suite لأن C4 يغير permission-protected chat flow وredaction-sensitive stored responses.
- `git diff --check`
  - تحقق whitespace.

نتائج الاختبارات:
- focused admin chat tests نجحت: 10 tests.
- Portal regression tests نجحت: 16 tests.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات.
- full test suite نجح: 310 tests, 4 skipped.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغل:
لا توجد اختبارات مطلوبة تم تجاوزها في هذا Sprint. تم تشغيل full suite كـ security/redaction regression gate.

المشاكل التي ظهرت وكيف تم حلها:
لا توجد مشاكل تنفيذية بعد تحديث C4؛ الاختبارات نجحت.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم الالتزام بقرارات `docs/planning/DECISION-REGISTER.md`.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`feat: add deterministic chat responder`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد نجاح الاختبارات والتحقق من عدم وجود migrations غير متوقعة.

هل يمكن الانتقال للـ Sprint التالي؟
نعم. يمكن الانتقال إلى `Sprint C5 - Tool Orchestrator MVP` بعد إنشاء commit مستقل لـ C4.

## Sprint C5 - Tool Orchestrator MVP

Sprint المنفذ:
`Sprint C5 - Tool Orchestrator MVP`

الهدف:
تمكين الشات من طلب وتشغيل أدوات موجودة فقط بعد موافقة owner/operator، مع المرور الإجباري عبر ToolDefinition وToolPolicy وPlanTool وToolRun وAgentJob.

ما تم إنجازه:
- إضافة `AdminChatToolRequest`.
- إضافة خدمة `create_tool_request()` لطلب أداة متاحة فقط من Safe Context.
- إضافة خدمة `approve_tool_request()` لاعتماد الطلب وتشغيله عبر `create_tool_run_job()` فقط.
- إضافة minimal Portal UI لطلب أداة واعتمادها داخل جلسة server-scoped.
- منع viewer من الاعتماد.
- منع params في C5 لتجنب arbitrary params.
- منع write-risk tools من الطلب.
- التأكد من أن ToolPolicy وPlanTool denial يحدثان قبل إنشاء ToolRun/AgentJob.
- عدم إضافة command/script template execution.

الملفات التي تم تعديلها:
- `apps/ai_chat/models.py`
- `apps/ai_chat/admin.py`
- `apps/ai_chat/services.py`
- `apps/ai_chat/migrations/0002_adminchattoolrequest.py`
- `apps/portal/views.py`
- `apps/portal/urls.py`
- `templates/portal/chat/detail.html`
- `tests/unit/test_admin_chat.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
نعم. تمت إضافة `apps/ai_chat/migrations/0002_adminchattoolrequest.py`.

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_admin_chat --noinput`
  - اختبارات مركزة لمسار request/approve والdenials وعدم bypass.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint6_portal tests.unit.test_sprint4_tools_policy --noinput`
  - regression للـ Portal والـ ToolPolicy لأن C5 يربط الشات بمسار tool execution.
- `.\.venv\Scripts\python.exe manage.py check`
  - تحقق Django العام.
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
  - للتأكد من عدم وجود migrations إضافية غير متوقعة بعد migration المقصودة.
- `.\.venv\Scripts\python.exe manage.py test --noinput`
  - تم تشغيل full suite لأن C5 يلمس ToolRun/AgentJob execution path وsecurity/policy enforcement.
- `git diff --check`
  - تحقق whitespace.

نتائج الاختبارات:
- focused admin chat tests نجحت: 16 tests.
- Portal/tools regression نجح: 31 tests.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات إضافية.
- full test suite نجح: 316 tests, 4 skipped.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغل:
لا توجد اختبارات مطلوبة تم تجاوزها. تم تشغيل full suite لأن السبرنت يمس مسار التنفيذ الأمني.

المشاكل التي ظهرت وكيف تم حلها:
- تم تشغيل regression أولًا باسم test module غير موجود `tests.unit.test_sprint4_tools`. تم تحديد الاسم الصحيح `tests.unit.test_sprint4_tools_policy` وإعادة التشغيل بنجاح.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم الالتزام بقرارات `docs/planning/DECISION-REGISTER.md`.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`feat: add chat tool orchestrator mvp`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد نجاح الاختبارات وإنشاء migration المقصودة فقط.

هل يمكن الانتقال للـ Sprint التالي؟
نعم. يمكن الانتقال إلى `Sprint C6 - Safe Command Execution Runtime` بعد إنشاء commit مستقل لـ C5.

## Sprint C6 - Safe Command Execution Runtime

Sprint المنفذ:
`Sprint C6 - Safe Command Execution Runtime`

الهدف:
إضافة أساس آمن لتشغيل `command_template` فقط داخل runtime باستخدام argv-only و`shell=False`، مع استمرار دعم runtime handlers الحالية.

ما تم إنجازه:
- إضافة `execution_type` إلى ToolTemplate وToolDefinition.
- إضافة `command_argv_template`.
- إضافة `allowed_binaries`.
- إضافة `blocked_tokens`.
- إضافة `AgentJob.execution_payload`.
- بناء payload آمن داخل `create_tool_run_job()` بعد ToolPolicy/PlanTool وقبل إنشاء AgentJob.
- رفض `script_template` في C6.
- رفض blocked tokens وbinaries غير المسموحة قبل إنشاء ToolRun/AgentJob.
- إضافة runtime executor للأوامر المعتمدة فقط في `scanner_runtime/command_templates.py`.
- تشغيل الأوامر عبر `safe_exec.run_fixed_command()` فقط، مع `shell=False`.
- إضافة `execution_time_seconds` و`truncated` إلى نتيجة safe_exec.
- الحفاظ على runtime-handler tools الحالية بدون إزالة.

الملفات التي تم تعديلها:
- `apps/tools/models.py`
- `apps/tools/services.py`
- `apps/tools/migrations/0006_tooldefinition_allowed_binaries_and_more.py`
- `apps/servers/models.py`
- `apps/servers/migrations/0005_agentjob_execution_payload.py`
- `scanner_runtime/safe_exec.py`
- `scanner_runtime/command_templates.py`
- `scanner_runtime/prototype.py`
- `tests/unit/test_sprint_c6_command_templates.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
نعم:
- `apps/tools/migrations/0006_tooldefinition_allowed_binaries_and_more.py`
- `apps/servers/migrations/0005_agentjob_execution_payload.py`

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint_c6_command_templates --noinput`
  - اختبارات مركزة لـ command-template payload/runtime/safe_exec.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint4_tools_policy tests.unit.test_sprint2_agent_foundation tests.unit.test_sprint3_bootstrap tests.unit.test_phase2_systemd_discovery --noinput`
  - regression للـ ToolPolicy وAgentJob وbootstrap runtime وruntime handler قديم.
- `.\.venv\Scripts\python.exe manage.py check`
  - تحقق Django العام.
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
  - للتأكد من عدم وجود migrations إضافية غير متوقعة بعد migrations المقصودة.
- `.\.venv\Scripts\python.exe manage.py test --noinput`
  - تم تشغيل full suite لأن C6 يغير runtime/job execution/security.
- `git diff --check`
  - تحقق whitespace.

نتائج الاختبارات:
- focused C6 tests نجحت: 8 tests.
- tools/agent/bootstrap/runtime regression نجح: 54 tests.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات إضافية.
- full test suite نجح: 324 tests, 4 skipped.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغل:
لا توجد اختبارات مطلوبة تم تجاوزها. تم تشغيل full suite لأنها بوابة execution/security.

المشاكل التي ظهرت وكيف تم حلها:
لا توجد مشاكل تنفيذية بعد C6؛ الاختبارات نجحت.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم استخدام التفويض المعتمد في C6 لتحديد migration shape.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`feat: add safe command template runtime`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد نجاح الاختبارات وإنشاء migrations المقصودة فقط.

هل يمكن الانتقال للـ Sprint التالي؟
نعم. يمكن الانتقال إلى `Sprint C7 - Tool Builder from Chat` بعد إنشاء commit مستقل لـ C6.

## Sprint C7 - Tool Builder from Chat

Sprint المنفذ:
`Sprint C7 - Tool Builder from Chat`

الهدف:
تمكين الشات من إنشاء `ToolBuildRequest` و`ToolBuildProposal` من نوع `command_template` فقط، مع بقاء المسار proposal-only وغير مفعّل.

ما تم إنجازه:
- إضافة metadata جديدة إلى `ToolBuildRequest` لدعم:
- `desired_execution_type`
- `command_argv_template`
- `allowed_binaries`
- `blocked_tokens`
- `expected_output_description_redacted`
- trace اختياري إلى `source_chat_session` و`source_chat_message`
- إضافة خدمة في `apps.ai_chat.services` لإنشاء builder request/proposal من الشات.
- توسيع Tool Builder الحالي في `apps.tools.services` لدعم `command_template` proposal:
- argv-only فقط
- binaries allowlisted فقط
- منع shell operators وredirects
- منع delete/write/restart/reload/package-install وقراءة `.env`
- منع `script_template`
- الحفاظ على سلوك `runtime_handler` الحالي بدون كسر.
- إضافة UI صغيرة داخل صفحة الشات لإنشاء proposal ومتابعة الحالة.
- عدم إنشاء ToolRun أو AgentJob وعدم تفعيل أي ToolDefinition.

الملفات التي تم تعديلها:
- `apps/tools/models.py`
- `apps/tools/services.py`
- `apps/tools/admin.py`
- `apps/tools/migrations/0007_toolbuildrequest_allowed_binaries_and_more.py`
- `apps/ai_chat/services.py`
- `apps/portal/views.py`
- `apps/portal/urls.py`
- `templates/portal/chat/detail.html`
- `tests/unit/test_sprint10_tool_builder.py`
- `tests/unit/test_admin_chat.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
نعم. تمت إضافة:
- `apps/tools/migrations/0007_toolbuildrequest_allowed_binaries_and_more.py`

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint10_tool_builder --noinput`
- لاختبار Tool Builder الحالي مع دعم `command_template` وعدم كسر مسار `runtime_handler`.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_admin_chat --noinput`
- لاختبار إنشاء builder proposal من الشات ومنع viewer.
- `.\.venv\Scripts\python.exe manage.py check`
- تحقق Django العام.
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
- للتأكد من عدم وجود migrations إضافية غير متوقعة بعد migration المقصودة.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_admin_chat tests.unit.test_sprint10_tool_builder tests.unit.test_sprint_c6_command_templates --noinput`
- regression مناسب لـ Chat + Tool Builder + C6 command-template layer.
- `git diff --check`
- تحقق whitespace.

نتائج الاختبارات:
- Tool Builder tests نجحت: 18 tests.
- Admin Chat tests نجحت: 19 tests.
- regression المجمّع نجح: 45 tests.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات إضافية.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغل:
- لم يتم تشغيل full suite.
- السبب: C7 لم يغيّر runtime execution semantics أو agent/job security path نفسه، بل بقي في proposal/chat layer.
- full suite سيعاد تشغيله قبل Internal Pilot وعند أي Sprint يمس runtime/agent/job execution/security مباشرة.

المشاكل التي ظهرت وكيف تم حلها:
- محاولة تشغيل اختبارين Django بالتوازي سببت تعارضًا في إنشاء test database PostgreSQL. تم التحول إلى تشغيل تسلسلي.
- validator كسر مسار `runtime_handler` القديم بسبب إضافة حقول command فارغة داخل payload. تم فصل branch `command_template` عن `runtime_handler`.
- `blocked_tokens` كانت تتعرض لـ redaction بسبب اسم المفتاح. تم تعديل `sanitize_builder_json` للحفاظ على مفاتيح config الآمنة مع استمرار redaction للقيم الحساسة.

القرارات الجديدة إن وجدت:
لا توجد قرارات جديدة. تم تنفيذ القرار المعتمد للخيار 2 فقط وبنطاق proposal-only.

تحديث LOG/CURRENT-TASKS:
تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit:
`feat: add chat tool builder proposals`

هل تم إغلاق Sprint بالكامل؟
نعم، بعد نجاح الاختبارات المطلوبة وإنشاء migration المقصودة فقط.

هل يمكن الانتقال للـ Sprint التالي؟
نعم. يمكن الانتقال إلى `Sprint C8 - First Laravel/Apache Tool Cycle` بعد إنشاء commit مستقل لـ C7.

## Sprint C8 - First Laravel/Apache Tool Cycle

الهدف:
- إثبات أول دورة أداة تجارية كاملة من الشات باستخدام `command_template` آمن وقراءة فقط.

ما تم إنجازه:
- إضافة أمر إدارة آمن لتمكين أداة `command_template` واحدة للـ pilot فقط:
  - `python manage.py enable_command_template_pilot_tool --plan-id <PLAN_ID> --tool-key <TOOL_KEY> [--dry-run]`
- فرض أن الأداة:
  - `read_only`
  - `command_template`
  - ناتجة من `ToolBuildProposal` تمت مراجعته وتحويله
- تفعيل `ToolPolicy` المناسب لتشغيل الأداة من الشات للـ `owner` و`operator` فقط، مع إبقاء التفعيل على الخطة المحددة فقط.
- إضافة تلخيص آمن لنتيجة `apache_5xx_summary` بدون عرض raw output.
- ربط نتيجة `ToolRun` النهائية بطلب الشات المرتبط:
  - تحديث حالة `AdminChatToolRequest`
  - إنشاء رسالة Assistant آمنة داخل الشات تشرح النتيجة
- توسيع الـ Safe Context والرد الحتمي لكي يشرح آخر نتيجة أداة بشكل آمن.
- دعم escaping محدود لـ `{{` و`}}` داخل argv template حتى يمكن تمثيل أوامر `awk` الثابتة دون shell ودون كسر نظام placeholders.

الملفات التي تم تعديلها:
- `apps/tools/command_template_enablement.py`
- `apps/tools/management/commands/enable_command_template_pilot_tool.py`
- `apps/tools/result_summaries.py`
- `apps/tools/services.py`
- `apps/ai_chat/services.py`
- `apps/ai_context/services.py`
- `tests/unit/test_sprint_c8_first_tool_cycle.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
- لا توجد migrations في هذا الـ Sprint.

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py check`
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint_c8_first_tool_cycle --noinput`
  - لاختبار مسار C8 نفسه: enablement + chat request + safe summary + permissions.
- `.\.venv\Scripts\python.exe manage.py test --noinput`
  - تم تشغيل full suite لأن C8 غيّر مسار execution/result propagation وربط النتيجة بالشات، وهذا يدخل ضمن سبرنتات runtime/agent/job execution/security حسب القاعدة المعتمدة.
- `git diff --check`

نتائج الاختبارات:
- اختبار C8 المركز نجح: 7 tests.
- full suite نجح: 338 tests, 4 skipped.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات إضافية.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغّل:
- لا توجد اختبارات إضافية متعمدة تم تخطيها بعد تشغيل full suite.

المشاكل التي ظهرت وكيف تم حلها:
- اختبار dry-run كان يفترض عدم وجود `ToolPolicy` مطلقًا، لكن التحويل من proposal ينشئ سياسة inactive أصلًا. تم تصحيح الاختبار ليتحقق من عدم حدوث تعديل، لا من عدم وجود السجل.
- argv الخاص بـ `awk` احتاج دعمًا آمنًا لـ escaped braces لأن المنفذ كان يفسر أي `{}` كـ placeholders. تم دعم `{{` و`}}` فقط ثم استعادة `{` و`}` بعد استبدال placeholders.
- أول تشغيل لـ full suite انتهى بزمن timeout من أداة التنفيذ بعد اكتمال الاختبارات فعليًا؛ أُعيد تشغيله بمهلة أعلى للحصول على exit code نظيف.

القرارات الجديدة إن وجدت:
- لا توجد قرارات جديدة. تم الالتزام بقرار C8 المعتمد:
  - بدء الدورة بأداة `apache_5xx_summary`
  - `command_template` فقط
  - بدون raw logs
  - بدون `laravel_env_sanity`

تحديث LOG/CURRENT-TASKS:
- تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit مقترح:
- `feat: add first apache command-template tool cycle`

هل تم إغلاق Sprint بالكامل؟
- نعم.

هل يمكن الانتقال للـ Sprint التالي؟
- نعم. يمكن الانتقال إلى `Sprint C9 - Reports from Chat`.

## Sprint C9 - Reports from Chat

الهدف:
- إضافة تقارير من داخل الشات عبر draft/review flow آمن، مع فصل نوعي التقرير:
  - `technical_internal`
  - `customer_summary`

ما تم إنجازه:
- إضافة نموذج `AdminChatReportDraft` داخل `ai_chat` ليكون مسار الـ draft الرسمي قبل أي `Report` نهائي.
- إضافة توليد حتمي وآمن لـ report drafts من Safe Context فقط، بدون أي live AI.
- دعم نوعين منفصلين من drafts:
  - `technical_internal`
  - `customer_summary`
- إضافة مسار Matrix Admin review:
  - approve
  - reject
  - convert to final `Report`
- توسيع `Report.ReportType` ليشمل:
  - `technical_internal`
  - `customer_summary`
- إضافة form بسيط داخل صفحة الشات في Portal لإنشاء report draft.
- إضافة history بسيط داخل صفحة الشات لعرض حالة drafts وربط التقرير النهائي بعد التحويل.
- إبقاء كل المحتوى معتمدًا فقط على:
  - Safe Context
  - redacted summaries
  - sections redacted
- منع أي raw ToolRun output أو raw AgentJob output أو raw logs أو raw `.env` من الوصول إلى draft أو التقرير النهائي الناتج من الشات.

الملفات التي تم تعديلها:
- `apps/ai_chat/models.py`
- `apps/ai_chat/services.py`
- `apps/ai_chat/admin.py`
- `apps/ai_chat/migrations/0003_adminchatreportdraft.py`
- `apps/reports/models.py`
- `apps/reports/migrations/0002_alter_report_report_type.py`
- `apps/portal/views.py`
- `apps/portal/urls.py`
- `templates/portal/chat/detail.html`
- `tests/unit/test_sprint_c9_chat_reports.py`
- `LOG.md`
- `docs/CURRENT-TASKS.md`
- `docs/planning/تقارير التنفيذ.md`

Migrations:
- نعم:
  - `apps/ai_chat/migrations/0003_adminchatreportdraft.py`
  - `apps/reports/migrations/0002_alter_report_report_type.py`

الاختبارات التي تم تشغيلها ولماذا:
- `.\.venv\Scripts\python.exe manage.py check`
- `.\.venv\Scripts\python.exe manage.py makemigrations ai_chat reports`
  - للتأكد من أن التعديلات الهيكلية المقصودة فقط هي التي نتجت.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint_c9_chat_reports --noinput`
  - لاختبار C9 نفسه: draft creation, viewer denial, admin review/conversion, redaction, account scoping.
- `.\.venv\Scripts\python.exe manage.py test tests.unit.test_sprint_c9_chat_reports tests.unit.test_admin_chat tests.unit.test_sprint11_reports --noinput`
  - regression مناسب لطبقة chat + reports الحالية.
- `.\.venv\Scripts\python.exe manage.py test --noinput`
  - تم تشغيل full suite لأن C9 يمس redaction, report visibility, report conversion, وPortal permissions.
- `.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`
- `git diff --check`

نتائج الاختبارات:
- اختبار C9 المركز نجح: 9 tests.
- regression المركّز نجح: 38 tests.
- full suite نجح: 347 tests, 4 skipped.
- `check` نجح.
- `makemigrations --check --dry-run` نجح بدون تغييرات إضافية.
- `git diff --check` نجح مع تحذيرات line endings فقط.

الاختبارات التي لم تُشغّل:
- لا توجد اختبارات إضافية متعمدة تم تخطيها بعد تشغيل full suite.

المشاكل التي ظهرت وكيف تم حلها:
- `convert_chat_report_draft()` كان يستدعي `_safe_text()` بدون `limit`. تم إصلاح الاستدعاء وإعادة تشغيل اختبارات C9.
- التحقق من التقرير النهائي احتاج توسيع `Report.ReportType` بدل تحميل النوعين الجديدين داخل draft فقط، لأن القرار المعتمد يطلب فصل النوعين كنوعين واضحين.

القرارات الجديدة إن وجدت:
- لا توجد قرارات جديدة.
- تم الالتزام فقط بالقرارات المعتمدة:
  - draft/review flow
  - تفضيل `AdminChatReportDraft`
  - لا raw outputs
  - لا PDF
  - لا live AI خارج المعتمد

تحديث LOG/CURRENT-TASKS:
- تم تحديث `LOG.md` و`docs/CURRENT-TASKS.md`.

Commit مقترح:
- `feat: add chat report draft review flow`

هل تم إغلاق Sprint بالكامل؟
- نعم.

هل يمكن الانتقال للـ Sprint التالي؟
- نعم. يمكن الانتقال إلى `Sprint C10 - Internal Pilot`.
